# Security headers, applied to every static/prerendered response (assets
# and the prerendered HTML). /api/* is server-rendered by the Worker and
# doesn't pass through here — see src/middleware.ts for its headers.
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), bluetooth=(), midi=(), magnetometer=(), gyroscope=(), accelerometer=(), display-capture=(), screen-wake-lock=(), xr-spatial-tracking=(), interest-cohort=(), browsing-topics=()
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; script-src 'self' https://www.youtube.com https://www.googletagmanager.com; img-src 'self' data: https://i.ytimg.com; connect-src 'self' https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://www.googletagmanager.com; frame-src https://www.youtube.com; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests

/
  Cache-Control: public, max-age=300, must-revalidate

/about
  Cache-Control: public, max-age=300, must-revalidate

/privacy
  Cache-Control: public, max-age=300, must-revalidate

/terms
  Cache-Control: public, max-age=300, must-revalidate

/contact
  Cache-Control: public, max-age=300, must-revalidate

/500
  Cache-Control: no-store

/_astro/*
  Cache-Control: public, max-age=31536000, immutable

/og.jpg
  Cache-Control: public, max-age=604800

/favicon.svg
  Cache-Control: public, max-age=604800

/apple-touch-icon.png
  Cache-Control: public, max-age=604800

/icon-*.png
  Cache-Control: public, max-age=604800

/robots.txt
  Cache-Control: public, max-age=3600

/manifest.webmanifest
  Cache-Control: public, max-age=86400
  Content-Type: application/manifest+json

# Service worker MUST stay uncached — the whole point of no-cache here
# is that updates land the next time the browser hits sw.js, not 10 min
# later. Keep as no-cache; anything else risks a stale SW pinning the
# site to old assets.
/sw.js
  Cache-Control: no-cache
  Content-Type: application/javascript; charset=utf-8

# The other static JS files aren't hashed, but they change rarely enough
# that a short-cache-then-revalidate is safe (and Lighthouse's "efficient
# cache lifetimes" rule fires at anything shorter than a few minutes).
# The service worker's stale-while-revalidate wraps this anyway, so the
# effective UX on updates is: served instantly from cache, then swapped
# in the background.
/player.js
  Cache-Control: public, max-age=3600, must-revalidate
  Content-Type: application/javascript; charset=utf-8

/sw-register.js
  Cache-Control: public, max-age=3600, must-revalidate
  Content-Type: application/javascript; charset=utf-8

/ga.js
  Cache-Control: public, max-age=3600, must-revalidate
  Content-Type: application/javascript; charset=utf-8

/nav.js
  Cache-Control: public, max-age=3600, must-revalidate
  Content-Type: application/javascript; charset=utf-8

/api/*
  Cache-Control: no-store
